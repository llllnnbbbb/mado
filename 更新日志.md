# 更新日志

按版本记录面向用户的功能与行为变化。开发环境、依赖安装等不记入本文件。

## 0.5.1（2026-09-22）

### 多窗口

- 当前窗口已经打开了 Markdown，或已有未保存内容时，再「新建」或「打开」会另开一个窗口，不再覆盖当前文档。
- 空白未命名窗口仍在本窗口打开，避免空窗口越开越多。
- 「关闭文件」只清空当前窗口，不关窗。
- 同一个文件若已在其它窗口打开，会切到那个窗口，不会再开一份。
- 从系统「打开方式」、第二次启动、或把 `.md` 拖进已占用的窗口时，同样按上述规则处理。
- 新窗口相对原窗口错开显示；主题切换会同步到其它已打开窗口。
